import React from 'react';
import '../styles/App.css';

export default class Home extends React.PureComponent {
    render() {
        return (
            <div>
                select option from side menu
            </div>
        );
    }
}